import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  btn: {
    borderWidth: 1,
    borderColor: '#fff',
    borderRadius: 20,
  },
  img: {
    width: 45,
    height: 45,
    tintColor: '#fff',
  },
});

export default styles;
